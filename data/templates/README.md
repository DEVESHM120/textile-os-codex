# Templates

Place reusable, sanitized sticker templates here.

The MVP can generate a production-style Excel artifact without an uploaded template. If a template is added later, the sticker service can map fabric fields into the template.
